Home for abstractions that any service can use.

Hence they do not have a right to use anything that is not defined and build inside its own folders.

anything here cannot use an external service, since any external service has a right to call this